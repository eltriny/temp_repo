# ============================================================================
# requirements.txt — pyproject.toml 포인터.
# ============================================================================
# 실제 의존성 정의는 pyproject.toml 의 [project.dependencies] 입니다 (SSOT).
# 개발 환경 설정:
#
#   pip install -e .[dev]
#
# CI / 재현 가능한 잠금이 필요할 경우 pip-compile 산출물을
# requirements.lock 으로 별도 관리할 것.
# ============================================================================

-e .
